/**
 * Named animation
 * Manages animation by name
 * @implements {Animation}
 * @classdesc Animation to manage animation by name
 */
class NamedAnimation extends Animation { // eslint-disable-line  no-unused-vars
    /**
     * Set animation name
     * @interface
     * @param {string} name Animation name
     */
    setName(name) {}
}
