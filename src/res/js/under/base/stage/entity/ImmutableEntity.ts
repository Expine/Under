import { InfluentialEntity } from "./InfluentialEntity";

/**
 * Immutable entity
 * - It is fixed and no change will occur
 * @abstract
 * @extends {InfluentialEntity}
 * @classdesc Immutable entity that is fixed and no change will occur
 */
export abstract class ImmutableEntity extends InfluentialEntity {

}
