/**
 * Parse file to stage
 * @classdesc Parser for stage
 */
class StageParser {
    /**
     * Parset file to stage
     * @interface
     * @param {string} filePath stage file path
     */
    parse(filePath) {}
}